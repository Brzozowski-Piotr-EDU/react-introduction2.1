interface QuoteProps {
  quote: string;
}

function Quote({ quote }: QuoteProps) {
  return (
    <div>
      <p></p>
      <p></p>
    </div>

    /*<button className={className} onClick={onClick}>
        {children}
      </button>*/
  );
}
